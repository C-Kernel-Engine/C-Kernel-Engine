{
  "header": {
    "headline": "Per Head Fusion Math",
    "subtitle": "Per-head fusion needs a technical view that keeps head-local math and merged artifact output aligned."
  },
  "nodes": {
    "stage_1": {
      "title": "Input State",
      "body": "Per Head Fusion Math starts from normalized or packed inputs that set the compute boundary.",
      "detail": [
        "per-head",
        "local fuse"
      ]
    },
    "stage_2": {
      "title": "Kernel Math",
      "body": "The middle of the diagram should expose the key transformation rather than hide it behind a generic box.",
      "detail": [
        "local fuse",
        "merge"
      ]
    },
    "stage_3": {
      "title": "Fused Path",
      "body": "Show how kernels fuse, gate, or rotate state before the output contract is formed.",
      "detail": [
        "fusion",
        "state update"
      ]
    },
    "stage_4": {
      "title": "Output Contract",
      "body": "The last stage should state what artifact or activation shape the runtime emits.",
      "detail": [
        "artifact output",
        "runtime state"
      ]
    }
  },
  "formula": {
    "title": "Technical Rule",
    "equation": "head_i = fuse(q_i, k_i, v_i)",
    "bullets": [
      "per-head",
      "local fuse",
      "merge"
    ]
  },
  "artifacts": {
    "left": {
      "title": "Math Trace",
      "body": "Preserve the local equation or activation story for per head fusion math.",
      "detail": [
        "equation",
        "activation path"
      ]
    },
    "right": {
      "title": "Runtime Trace",
      "body": "Show the resulting kernel artifact, buffer change, or residual handoff.",
      "detail": [
        "buffer handoff",
        "kernel artifact"
      ]
    }
  },
  "footer": {
    "note": "Per Head Fusion Math belongs in the broader contract because the model needs dense technical layouts, not only charts and posters."
  }
}
