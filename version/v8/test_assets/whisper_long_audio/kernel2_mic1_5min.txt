Now, in the first video in the series, I started with activation, activation kernels, and we looked at sigmoid, ReLU, ReLU squared, SiLU, and SwiGLU. These are all the different activations, and the objective was to connect each mathematical formula to its curves, its role in a model, and the loop a computer eventually has to execute. And in this video I want to continue from there, but widen the view. I want to build a practical map of the kernels that appear across modern AI systems. Like, what is a GEMM? And how do normalization, softmax, RoPE, rotary positional encoding, quantization, attention, and recurrent states become loops and memory access on a real CPU?

And I'm creating the series for two reasons. First, I want to improve my own understanding. And building C-Kernel-Engine forces me to move between model architecture, mathematics, C code, compiler output, numerical parity, and physical CPU measurements. And explaining that work makes the gap in my understanding much easier to find. I've been doing the series for a while now, so it just becomes more and more ingrained in my mind.

But the second most important factor, a little selfish reason, is also CKE needs more contributors and also more CPU evidence. So you do not need a GPU cluster to contribute. So if you have a Linux computer with an Intel, AMD, or even an ARM CPU, that machine can expose a different instruction set, cache hierarchy, memory limit, compiler behavior, or scheduling problems. So I hopefully can encourage more contributors to contribute to the project and become a better kernel engineer, improve on your understanding of how AI works at the fundamental level. And working on frameworks is my kind of, like, my gateway, was my recommended gateway for you to understand these things much more practically and much more, what do you say, viscerally when it comes to AI.

So you can begin by reproducing one test and inspect one kernel, compare it with its scalar reference, profile it, make one change, and prove that the results remain correct. Now CKE, you know, at this point in time does have a very, very broad model compatibility, different quantization formats, not all, but some of it. But in this video I'm still talking more about, you know, a follow-up from what I did last time, which was all about understanding how to become a kernel engineer.

And that work also strengthens your career, hopefully. So instead of only saying that you know C or Linux or single instruction multiple data, how to profile, or machine learning, you can now point to a real kernel, a benchmark, a parity result, and a pull request. And that is concrete engineering evidence.

So if you're a beginner, do not begin by trying to understand the entire model at once. Pick one operation. Write down its equation. Track its tensor shapes, and then implement the simplest correct version, and then test tiny inputs. Then ask: what is the CPU doing with every load, store, loop, vector lane, and thread? And do not optimize because the code looks clever. Like, first measure it and keep the scalar implementation as an oracle. You could also use PyTorch or something that I've been using as an oracle. Then change one boundary at a time and check correctness again after every optimization.

This is how I want the series to work. So one mathematical operation at a time, connected to real code, real hardware, and real evidence. And let's continue with the series. So I'm Anthony Shivakumar from Antshiv Robotics. I do build C-Kernel-Engine. I'm looking for contributors. I do document some of my blogs on ShivasNotes.com. So let us continue from those activation kernels that I spoke about in my previous video, and let's get started.

I'm going to put it in the middle. So here is the part for this video: math, kernels, runtime, and model. And when I say kernels, I do not mean the Linux kernel. I mean a focused routine that computes one operation or one tightly fused group of
