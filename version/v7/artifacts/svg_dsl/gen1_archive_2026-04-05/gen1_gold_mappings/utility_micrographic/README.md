# Gen1 Gold Mappings: utility_micrographic

These are compiler-first utility-micrographic seeds for the broader gen1 contract.

- family: `utility_micrographic`
- contract: `scene.compact.dsl + content.json -> utility_micrographic renderer -> SVG`
- status: `seeded_for_gen1_full_mix`

