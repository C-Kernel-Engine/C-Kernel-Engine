{
  "id": "recurrent_split_qkv_backward_f32",
  "op": "recurrent_split_qkv_backward",
  "variant": "fp32",
  "quant": {
    "weight": "none",
    "activation": "fp32",
    "output": "fp32"
  },
  "inputs": [
    {"name": "d_q", "dtype": "fp32", "shape": ["R", "Q"], "desc": "Gradient of split query rows"},
    {"name": "d_k", "dtype": "fp32", "shape": ["R", "K"], "desc": "Gradient of split key rows"},
    {"name": "d_v", "dtype": "fp32", "shape": ["R", "V"], "desc": "Gradient of split value rows"}
  ],
  "weights": [],
  "activations": [],
  "outputs": [
    {"name": "d_packed_qkv", "dtype": "fp32", "shape": ["R", "Q+K+V"], "desc": "Gradient of packed recurrent QKV rows"}
  ],
  "scratch": [],
  "dims": ["R", "Q", "K", "V"],
  "params": [],
  "parallelization": {
    "supported": ["row"],
    "preferred": {"prefill": "row", "decode": "row"},
    "strategies": [
      {
        "name": "row",
        "partition_dim": "R",
        "param_style": "range",
        "range": {"min_chunk": 1},
        "notes": "Each packed row gradient can be reconstructed independently."
      }
    ]
  },
  "constraints": {
    "notes": "Backward for the recurrent packed QKV split."
  },
  "impl": {
    "function": "recurrent_split_qkv_backward",
    "c_declaration": "void recurrent_split_qkv_backward(const float *d_q, const float *d_k, const float *d_v, float *d_packed_qkv, int rows, int q_dim, int k_dim, int v_dim);",
    "sources": ["src/kernels/recurrent_split_kernels.c"],
    "variants": [
      {"name": "ref", "requires": [], "compile_flags": []}
    ]
  },
  "tests": {
    "unit": ["unittest/test_recurrent_split_qkv.py"],
    "parity": [
      {
        "kind": "pytorch",
        "command": ".venv/bin/python unittest/test_recurrent_split_qkv.py",
        "tolerance": "0",
        "notes": "Backward parity against torch.split autograd."
      }
    ]
  },
  "notes": "Backward kernel for the recurrent packed QKV split."
}
