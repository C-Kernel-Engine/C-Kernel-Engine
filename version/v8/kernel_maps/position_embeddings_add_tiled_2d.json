{
  "id": "position_embeddings_add_tiled_2d",
  "op": "position_embeddings",
  "variant": "absolute_learned_inplace_tiled_2d_fp32",
  "quant": {
    "weight": "fp32",
    "activation": "fp32",
    "output": "fp32"
  },
  "inputs": [
    {
      "name": "x",
      "dtype": "fp32",
      "shape": ["T", "E"],
      "desc": "Token-major activation stream already arranged in merged-tile order"
    }
  ],
  "weights": [
    {
      "name": "pos_emb",
      "dtype": "fp32",
      "shape": ["P", "E"],
      "desc": "Learned position embeddings stored in row-major patch order"
    }
  ],
  "outputs": [
    {
      "name": "x",
      "dtype": "fp32",
      "shape": ["T", "E"],
      "desc": "In-place updated activation stream"
    }
  ],
  "dims": ["grid_h", "grid_w", "embed_dim", "merge_size"],
  "impl": {
    "function": "position_embeddings_add_tiled_2d",
    "c_declaration": "void position_embeddings_add_tiled_2d(float *x, const float *position_embd, int grid_h, int grid_w, int embed_dim, int merge_size);",
    "sources": ["src/kernels/vision_kernels.c"]
  },
  "notes": "Add learned absolute position embeddings to a stream that has already been reordered into merged-tile traversal."
}
