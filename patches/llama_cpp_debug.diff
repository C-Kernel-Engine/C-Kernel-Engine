diff --git a/examples/eval-callback/eval-callback.cpp b/examples/eval-callback/eval-callback.cpp
index 408338f1a..11d203e51 100644
--- a/examples/eval-callback/eval-callback.cpp
+++ b/examples/eval-callback/eval-callback.cpp
@@ -8,6 +8,7 @@
 #include <cstdio>
 #include <string>
 #include <vector>
+#include <sys/stat.h>
 
 /**
  * This the arbitrary data which will be passed to each callback.
@@ -152,9 +153,43 @@ static bool ggml_debug(struct ggml_tensor * t, bool ask, void * user_data) {
         ggml_backend_tensor_get(t, cb_data->data.data(), 0, n_bytes);
     }
 
+    auto should_dump = [](const char * name) {
+        if (!name || !name[0]) {
+            return false;
+        }
+        const size_t len = strlen(name);
+        if (len >= 2 && name[len - 2] == '-' && name[len - 1] == '0') {
+            return true;
+        }
+        if (strstr(name, "output") || strstr(name, "result") ||
+            strstr(name, "embd") || strstr(name, "inp_")) {
+            return true;
+        }
+        return false;
+    };
+
+    uint8_t * data = is_host ? (uint8_t *) t->data : cb_data->data.data();
+
+    if (should_dump(t->name)) {
+        struct stat st = {0};
+        if (stat("llama_dump", &st) == -1) {
+            mkdir("llama_dump", 0700);
+        }
+        char fname[512];
+        snprintf(fname, sizeof(fname), "llama_dump/%s.bin", t->name);
+        FILE * f = fopen(fname, "wb");
+        if (f) {
+            fwrite(data, 1, ggml_nbytes(t), f);
+            fclose(f);
+            LOG("[DUMP] %s (%zu bytes)\n", t->name, ggml_nbytes(t));
+        }
+    }
+
     if (!ggml_is_quantized(t->type)) {
-        uint8_t * data = is_host ? (uint8_t *) t->data : cb_data->data.data();
-        ggml_print_tensor(data, t->type, t->ne, t->nb, 3);
+        const char * env_print = getenv("CK_LLAMA_PRINT");
+        if (env_print && env_print[0] != '\0') {
+            ggml_print_tensor(data, t->type, t->ne, t->nb, 3);
+        }
     }
 
     return true;
diff --git a/ggml/src/ggml-cpu/ggml-cpu.c b/ggml/src/ggml-cpu/ggml-cpu.c
index f7ba1fe31..da62ffeb4 100644
--- a/ggml/src/ggml-cpu/ggml-cpu.c
+++ b/ggml/src/ggml-cpu/ggml-cpu.c
@@ -27,6 +27,7 @@
 #include <math.h>
 #include <stdlib.h>
 #include <string.h>
+#include <sys/stat.h>
 #include <stdint.h>
 #include <inttypes.h>
 #include <stdio.h>
@@ -2944,8 +2945,30 @@ static thread_ret_t ggml_graph_compute_thread(void * data) {
         }
 
         ggml_compute_forward(&params, node);
-
-        if (state->ith == 0 && cplan->abort_callback &&
+if (state->ith == 0 && node->name[0] != 0) {
+            // Dump layer 0 intermediate tensors for parity testing
+            // Layer 0 tensors end with "-0" (e.g., Qcur-0, attn_norm-0)
+            const char *name = node->name;
+            int dump_it = 0;
+            // Check for layer 0 suffix (-0)
+            size_t len = strlen(name);
+            if (len >= 2 && name[len-2] == '-' && name[len-1] == '0') dump_it = 1;
+            // Also dump embedding, final outputs
+            if (strstr(name, "output") || strstr(name, "result") ||
+                strstr(name, "embd") || strstr(name, "inp_")) dump_it = 1;
+            if (dump_it) {
+                struct stat st = {0};
+                if (stat("llama_dump", &st) == -1) { mkdir("llama_dump", 0700); }
+                char fname[512];
+                snprintf(fname, sizeof(fname), "llama_dump/%s.bin", name);
+                FILE *f = fopen(fname, "wb");
+                if (f) {
+                    fwrite(node->data, 1, ggml_nbytes(node), f);
+                    fclose(f);
+                    fprintf(stderr, "[DUMP] %s (%zu bytes)\n", name, ggml_nbytes(node));
+                }
+            }
+        }        if (state->ith == 0 && cplan->abort_callback &&
                 cplan->abort_callback(cplan->abort_callback_data)) {
             atomic_store_explicit(&tp->abort, node_n + 1, memory_order_relaxed);
             tp->ec    = GGML_STATUS_ABORTED;
diff --git a/tools/completion/completion.cpp b/tools/completion/completion.cpp
index a9eda119d..9a85503ba 100644
--- a/tools/completion/completion.cpp
+++ b/tools/completion/completion.cpp
@@ -7,6 +7,7 @@
 #include "chat.h"
 
 #include <cstdio>
+#include <cstdlib>
 #include <cstring>
 #include <ctime>
 #include <fstream>
@@ -41,6 +42,32 @@ static std::vector<llama_token> * g_output_tokens;
 static bool is_interacting  = false;
 static bool need_insert_eot = false;
 
+static void dump_prompt_tokens_if_enabled(const std::vector<llama_token> & tokens) {
+    const char * dump_env = std::getenv("LLAMA_DUMP_TOKENS");
+    if (!dump_env || dump_env[0] == '0') {
+        return;
+    }
+
+    const char * dir_env = std::getenv("LLAMA_DUMP_DIR");
+    const std::string dump_dir = (dir_env && dir_env[0] != '\0') ? dir_env : "llama_dump";
+    fs_create_directory_with_parents(dump_dir);
+
+    const std::string path = dump_dir + "/prompt_tokens.json";
+    std::ofstream out(path);
+    if (!out) {
+        return;
+    }
+
+    out << "[";
+    for (size_t i = 0; i < tokens.size(); ++i) {
+        if (i) {
+            out << ", ";
+        }
+        out << tokens[i];
+    }
+    out << "]";
+}
+
 static void print_usage(int argc, char ** argv) {
     (void) argc;
 
@@ -420,6 +447,10 @@ int main(int argc, char ** argv) {
         LOG_INF("\n");
     }
 
+    if (!embd_inp.empty()) {
+        dump_prompt_tokens_if_enabled(embd_inp);
+    }
+
     // ctrl+C handling
     {
 #if defined (__unix__) || (defined (__APPLE__) && defined (__MACH__))
@@ -530,6 +561,7 @@ int main(int argc, char ** argv) {
     int n_remain           = params.n_predict;
     int n_consumed         = 0;
     int n_session_consumed = 0;
+    const bool prompt_only = (params.n_predict == 0 && !params.interactive);
 
     std::vector<int>   input_tokens;  g_input_tokens  = &input_tokens;
     std::vector<int>   output_tokens; g_output_tokens = &output_tokens;
@@ -570,7 +602,7 @@ int main(int argc, char ** argv) {
         embd_inp.push_back(decoder_start_token_id);
     }
 
-    while ((n_remain != 0 && !is_antiprompt) || params.interactive) {
+    while ((n_remain != 0 && !is_antiprompt) || params.interactive || prompt_only) {
         // predict
         if (!embd.empty()) {
             // Note: (n_ctx - 4) here is to match the logic for commandline prompt handling via
@@ -698,6 +730,10 @@ int main(int argc, char ** argv) {
 
         embd.clear();
 
+        if (prompt_only && (int) embd_inp.size() <= n_consumed && !is_interacting) {
+            break;
+        }
+
         if ((int) embd_inp.size() <= n_consumed && !is_interacting) {
             // optionally save the session on first sample (for faster prompt loading next time)
             if (!path_session.empty() && need_to_save_session && !params.prompt_cache_ro) {
